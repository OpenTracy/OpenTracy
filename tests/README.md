# tests/

- `unit/` — per-module runtime tests (registry, loader, store, loop)
- `evals/` — offline eval harness: scenario files (task + fixture tools + expected
  behavior) scored on tool-choice, skill-activation, and memory-recall correctness (Phase 5)
